#include <iostream>
#include <cmath>
#include <cstdlib>
#include "ModulesAl-Ghazali.h" // Підключаємо заголовок вашої бібліотеки

using namespace std;

int main() {
    // Налаштування консолі для коректного виведення українських літер в Windows
    #ifdef _WIN32
    system("chcp 65001 > nul");
    #endif

    cout << "=========================================================" << endl;
    cout << " Лабораторна робота № 9. Завдання 9.4" << endl;
    cout << " Виконав: Султан Аль-Газали (Заочна форма)" << endl;
    cout << " Програма розв'язання задачі 9.4 з використанням бібліотек" << endl;
    cout << "=========================================================" << endl << endl;

    double x, y;
    int n;

    // Ввод для задачи 9.1
    cout << "[ Завдання 9.1 - Обчислення розгалуження ]" << endl;
    cout << "Введіть значення змінної x: ";
    if (!(cin >> x)) { cout << "Помилка введення x!" << endl; return 1; }
    cout << "Введіть значення змінної y: ";
    if (!(cin >> y)) { cout << "Помилка введення y!" << endl; return 1; }

    double res_9_1 = compute_task_9_1(x, y);
    cout << "Результат обчислення розгалуження: " << res_9_1 << endl;
    cout << "---------------------------------------------------------" << endl;

    // Ввод для задачи 9.2
    cout << "[ Завдання 9.2 - Обчислення суми ряду (цикл) ]" << endl;
    cout << "Введіть кількість елементів ряду (n): ";
    if (!(cin >> n) || n <= 0) { cout << "Помилка: n має бути більшим за 0!" << endl; return 1; }

    double res_9_2 = compute_task_9_2(n);
    cout << "Сума гармонічного ряду для n = " << n << " становить: " << res_9_2 << endl;
    cout << "=========================================================" << endl;

    return 0;
}
