#include <iostream>
#include <cmath>
#include <cstdlib>
#include "ModulesAl-Ghazali.h" // Подключаем заголовок вашей библиотеки

using namespace std;

int main() {
    // Настройка консоли для корректного вывода украинских букв в Windows
    #ifdef _WIN32
    system("chcp 65001 > nul");
    #endif

    cout << "=========================================================" << endl;
    cout << " Лабораторна робота № 8. Завдання 8.2" << endl;
    cout << " Виконав: Султан Аль-Газали (Заочна форма)" << endl;
    cout << " Програма розв'язання задачі 8.2 з використанням бібліотек" << endl;
    cout << "=========================================================" << endl << endl;

    double x, y, z;

    cout << "Введіть значення змінної x: ";
    if (!(cin >> x)) {
        cout << "Помилка: Некоректне введення значення x!" << endl;
        return 1;
    }

    cout << "Введіть значення змінної y: ";
    if (!(cin >> y)) {
        cout << "Помилка: Некоректне введення значення y!" << endl;
        return 1;
    }

    cout << "Введіть значення змінної z: ";
    if (!(cin >> z)) {
        cout << "Помилка: Некоректне введення значення z!" << endl;
        return 1;
    }

    // Вызываем функцию расчета из нашей статической библиотеки
    double final_result = s_calculation(x, y, z);

    // Если функция вернула сигнальное значение ошибки ОДЗ
    if (final_result == -999.99) {
        cout << "\n[ ПОМИЛКА ]: Порушено ОДЗ! Вираз під коренем від'ємний." << endl;
        cout << "=========================================================" << endl;
        return 1;
    }

    cout << "\n--- Результати системного розрахунку задачі 8.2 ---" << endl;
    cout << "Вхідні параметри: x = " << x << ", y = " << y << ", z = " << z << endl;
    cout << "Отриманий результат виразу: " << final_result << endl;
    cout << "=========================================================" << endl;

    return 0;
}

